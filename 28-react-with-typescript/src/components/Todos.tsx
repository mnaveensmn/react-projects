
type TodoData = {
    items: string[]
}
const Todos: React.FC<TodoData> = (props) => {
  return (
    <ul>
      {props.items.map((item) => (
        <li key={item}>{item}</li>
      ))}
    </ul>
  );
};

export default Todos;
